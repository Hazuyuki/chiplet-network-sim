# NVSwitch拓扑快速测试指南

## 快速开始

### 方法1: 使用测试配置文件（最简单）

```bash
# 1. 编译项目
cd chiplet-network-sim
cmake --preset Release
cd builds/Release/
cmake --build .

# 2. 运行测试配置
./ChipletNetworkSim ../../input/nvswitch_test.ini
```

如果看到类似以下输出，说明拓扑创建成功：
```
NVSwitch Topology: 1 groups, 4 GPUs/group, 3 switches/group, switch_radix=10
Number of cores: 4
Number of nodes: 7
...
```

### 方法2: 编译并运行完整测试套件

```bash
# 1. 配置CMake启用测试
cd chiplet-network-sim
cmake --preset Release -DBUILD_NVSWITCH_TEST=ON
cd builds/Release/

# 2. 编译测试程序
cmake --build . --target test_nvswitch

# 3. 运行测试
./test_nvswitch
```

### 方法3: 编译并运行简化测试

```bash
# 1. 配置CMake启用测试
cmake --preset Release -DBUILD_NVSWITCH_TEST=ON
cd builds/Release/

# 2. 编译简化测试
cmake --build . --target test_nvswitch_simple

# 3. 运行测试
./test_nvswitch_simple
```

### 方法4: 使用测试脚本

```bash
cd chiplet-network-sim
./test_nvswitch.sh
```

## 测试检查清单

运行测试后，检查以下内容：

### ✓ 拓扑创建
- [ ] 输出显示 "NVSwitch Topology"
- [ ] GPU数量正确
- [ ] Switch数量正确
- [ ] 节点总数 = GPU数 + Switch数

### ✓ 连接验证
- [ ] 每个GPU连接到所有Switch
- [ ] Switch之间全连接（如果启用）
- [ ] 没有连接错误

### ✓ 路由功能
- [ ] 数据包可以从GPU发送到GPU
- [ ] 路由算法正常工作
- [ ] 没有路由错误

### ✓ 数据包传输
- [ ] 数据包可以成功传输
- [ ] 传输延迟合理
- [ ] 没有死锁

## 常见问题

### Q: 编译错误 "未定义的引用"
**A:** 确保所有源文件都包含在编译中，检查CMakeLists.txt

### Q: 运行时错误 "找不到配置文件"
**A:** 确保在正确的目录运行，或使用绝对路径

### Q: 测试失败 "断言失败"
**A:** 检查：
1. Switch radix是否足够大
2. 配置参数是否合理
3. 连接逻辑是否正确

### Q: 数据包传输超时
**A:** 可能的原因：
1. 路由算法有问题
2. 缓冲区配置太小
3. VC数量不足

## 手动验证步骤

如果自动测试通过，可以手动验证：

### 1. 检查拓扑结构
```bash
./ChipletNetworkSim ../../input/nvswitch_test.ini 2>&1 | grep -i "topology\|gpu\|switch\|node"
```

### 2. 检查统计信息
运行后查看输出文件（如果配置了）：
```bash
cat ../../output/nvswitch_test.csv
```

### 3. 测试不同配置
修改 `input/nvswitch_test.ini` 中的参数，测试不同配置：
- 改变GPU数量
- 改变Switch数量
- 改变路由算法
- 启用/禁用Switch全连接

## 性能基准测试

对于性能测试，可以使用更大的配置：

```ini
[Network]
num_gpus_per_group = 8
num_switches_per_group = 6
num_groups = 2
switch_radix = 18
```

然后运行并观察：
- 模拟时间
- 内存使用
- CPU使用率

## 下一步

测试通过后，可以：
1. 集成到CI/CD流程
2. 添加更多测试用例
3. 进行性能分析
4. 优化路由算法
