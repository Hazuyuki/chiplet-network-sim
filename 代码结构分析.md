# Chiplet Network Simulator (CNSim) 代码结构分析

## 项目概述

**Chiplet Network Simulator (CNSim)** 是一个周期精确的包并行模拟器，用于高效模拟大规模基于Chiplet的网络。该项目支持多线程、周期精确模拟，并在拓扑、路由算法和微架构方面高度可配置。

## 整体架构

### 核心设计模式
- **分层架构**: System → Group → Node → Buffer
- **工厂模式**: System::New() 根据配置创建不同的拓扑系统
- **多线程并行**: 支持多线程包并行处理，提高模拟效率

## 目录结构

```
chiplet-network-sim/
├── src/                    # 源代码目录
│   ├── main.cpp           # 程序入口，主循环和线程管理
│   ├── system.h/cpp       # 系统抽象基类，路由器流水线
│   ├── group.h/cpp        # 组抽象类（如chiplet、package）
│   ├── node.h/cpp         # 节点类（路由器/核心）
│   ├── buffer.h/cpp       # 输入缓冲区，VC管理
│   ├── packet.h/cpp       # 数据包类
│   ├── config.h/cpp       # 配置参数管理
│   ├── traffic_manager.h/cpp  # 流量生成和统计
│   ├── topologies/        # 各种网络拓扑实现
│   ├── traffic/           # 特定流量模式实现
│   └── netrace/           # Netrace支持（可选）
├── input/                 # 配置文件目录
└── CMakeLists.txt         # 构建配置
```

## 核心类层次结构

### 1. System（系统层）
**位置**: `src/system.h/cpp`

**职责**:
- 网络系统的顶层抽象
- 管理多个Group（组）
- 实现路由器流水线（OneStage/TwoStage/ThreeStage）
- 路由算法接口（由子类实现）

**关键方法**:
- `System::New()`: 工厂方法，根据拓扑名称创建具体系统
- `update(Packet&)`: 更新单个数据包状态
- `routing()`, `vc_allocate()`, `switch_allocate()`: 路由器三阶段

**支持的拓扑**:
- SingleChipMesh（单芯片Mesh）
- MultiChipMesh（多芯片Mesh）
- MultiChipTorus（多芯片Torus）
- DragonflySW（Dragonfly交换机）
- DragonflyChiplet（Dragonfly Chiplet）
- RailX2DHyperX（RailX 2D HyperX）
- RailX2DTorus（RailX 2D Torus）
- RailX2DTwistedTorus（RailX 2D 扭曲Torus）
- FatTree（胖树）
- HammingMesh（汉明Mesh）

### 2. Group（组层）
**位置**: `src/group.h/cpp`

**职责**:
- 系统的中间层抽象
- 在多芯片系统中，Group通常代表一个chiplet或package
- 管理一组Node（节点）

**关键属性**:
- `nodes_`: 节点向量
- `num_nodes_`: 节点数量
- `num_cores_`: 核心数量（可发送/接收数据包）

### 3. Node（节点层）
**位置**: `src/node.h/cpp`

**职责**:
- 网络的基本构建块
- 包含多个输入端口（Buffer）
- 管理与其他节点的连接

**关键属性**:
- `in_buffers_`: 输入缓冲区向量
- `link_nodes_`: 连接的节点ID
- `link_buffers_`: 连接的缓冲区指针
- `radix_`: 节点度数

### 4. Buffer（缓冲层）
**位置**: `src/buffer.h/cpp`

**职责**:
- 路由器的输入端口
- 管理虚拟通道（VC）
- 处理缓冲区分配和链路分配

**关键方法**:
- `allocate_buffer()`: 分配VC缓冲区
- `allocate_in_link()`: 分配输入链路
- `allocate_sw_link()`: 分配交换机链路
- `is_empty()`: 检查VC是否为空

**关键属性**:
- `vc_num_`: 虚拟通道数量
- `buffer_size_`: 每个VC的缓冲区大小（以flit为单位）
- `channel_`: 物理链路（带宽和延迟）

### 5. Packet（数据包）
**位置**: `src/packet.h/cpp`

**职责**:
- 表示网络中的数据包
- 跟踪每个flit在网络中的位置
- 管理路由状态

**关键属性**:
- `flit_trace_`: 每个flit的位置跟踪（VCInfo向量）
- `candidate_channels_`: 路由候选通道
- `next_vc_`: 下一跳的VC
- `length_`: 数据包长度（flit数）
- 各种计时器：`link_timer_`, `wait_timer_`, `trans_timer_`等

### 6. TrafficManager（流量管理）
**位置**: `src/traffic_manager.h/cpp`

**职责**:
- 生成各种流量模式
- 收集和打印统计信息
- 管理Netrace集成

**支持的流量模式**:
- uniform（均匀随机）
- hotspot（热点）
- bitcomplement, bitreverse, bitshuffle, bittranspose（位操作模式）
- adversarial（对抗性）
- sd_trace（SD trace）
- netrace（Netrace格式）
- ring_all_reduce, ring_all_reduce_bi（集合通信）
- collective（集合操作）

### 7. Parameters（配置参数）
**位置**: `src/config.h/cpp`

**职责**:
- 从INI文件读取配置
- 存储所有模拟参数

**关键参数**:
- 网络参数：拓扑、缓冲区大小、VC数量、路由器阶段数
- 工作负载：流量模式、数据包长度
- 模拟参数：模拟时间、注入率、超时阈值、线程数

## 路由器流水线

### OneStage（单阶段）
所有阶段（路由、VC分配、交换机分配）在一个周期内完成

### TwoStage（两阶段）
- 阶段1：路由 + VC分配
- 阶段2：交换机分配

### ThreeStage（三阶段）
- 阶段1：路由
- 阶段2：VC分配
- 阶段3：交换机分配

## 多线程架构

**位置**: `src/main.cpp`

**设计**:
- 主线程：生成数据包，协调周期
- 工作线程：并行更新数据包状态
- 使用原子操作和条件变量同步

**关键机制**:
- `pkt_i`: 原子索引，指向下一个要处理的数据包
- `issue_width`: 每个线程一次获取的数据包数量
- `worker_launch[]`: 工作线程启动信号
- `cv`: 条件变量，用于线程同步

**工作流程**:
1. 主线程生成数据包
2. 每个周期：
   - 释放链路状态，删除已到达的数据包（单线程）
   - 并行更新所有数据包（多线程）

## 数据包更新流程

1. **到达输入缓冲区** (`link_timer_ == 0`)
   - 如果未到达目的地：执行路由流水线
   - 如果到达目的地：标记完成

2. **路由流水线**
   - 路由：计算候选通道
   - VC分配：尝试分配虚拟通道
   - 交换机分配：分配物理链路

3. **传输**
   - 如果分配成功：更新flit跟踪，减少链路计时器
   - 如果分配失败：增加等待计时器

4. **Flit跟踪更新**
   - 根据链路宽度更新flit位置
   - 释放已离开的缓冲区

## 拓扑实现示例

### SingleChipMesh
- `NodeMesh`: 2D Mesh节点（xneg, xpos, yneg, ypos端口）
- `ChipMesh`: 芯片组
- `SingleChipMesh`: 单芯片Mesh系统
- 路由算法：XY路由、NFR路由、NFR自适应路由

## 配置文件格式

INI格式，包含以下部分：
- `[Network]`: 网络拓扑参数
- `[Workload]`: 工作负载参数
- `[Simulation]`: 模拟参数
- `[Files]`: 文件路径

## 编译和运行

### 依赖
- Boost库
- Netrace（可选）
- bzip2（如果使用Netrace）

### 构建
```bash
cmake --preset Release
cd builds/Release/
cmake --build .
./ChipletNetworkSim ../../input/config.ini
```

## 关键设计特点

1. **周期精确**: 每个周期精确模拟网络行为
2. **包并行**: 多个数据包并行处理
3. **多线程**: 利用多核CPU加速模拟
4. **可扩展**: 支持大规模网络模拟
5. **可配置**: 高度可配置的拓扑、路由和微架构
6. **模块化**: 清晰的层次结构，易于扩展

## 扩展点

1. **添加新拓扑**: 继承System类，实现`read_config()`和`routing_algorithm()`
2. **添加新路由算法**: 在System子类中实现新的路由方法
3. **添加新流量模式**: 在TrafficManager中添加新的生成方法
4. **修改路由器微架构**: 修改System中的流水线方法

## 统计信息

TrafficManager收集的统计信息：
- 平均延迟
- 接收率
- 总跳数（内部/外部/特定/其他）
- 消息到达数
- 消息超时数
